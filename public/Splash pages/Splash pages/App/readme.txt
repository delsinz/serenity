Do not change the directory structure, you will break the code

If you're going to change directory structure, you're going to need to update file paths in:

index.html
theteam.html
carousel.js

Install the required node modules using webpack.config.js

You'll need to link the login parts with the rest of the page, 

login and register components found in app.jsx, it's react code